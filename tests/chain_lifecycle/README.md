# Chain Lifecycle Tests

Chain construction, validation, serialization, reflection, cancellation, evolutionary search (`ChainEvolver`), checkpoint/resume, and the test-harness API.

## Files

- `test_care_metadata.py` — Tests for chain content metadata convention.
- `test_chain_evolution.py` — Tests for ``ChainMutator`` and ``ChainEvolver`` (evolutionary chain search).
- `test_chain_from_description.py` — Tests for ``ChainBuilder.from_description(task, llm_client, ...)``.
- `test_chain_roundtrip_all_step_types.py` — Round-trip every ``StepType`` value through ``chain.to_dict()`` → ``ReasoningChain.from_dict(use_typed_steps=True)``.
- `test_chain_visualizer.py` — Tests for ``ChainVisualizer`` — chainable facade over all CARL viz methods.
- `test_evolver_checkpoint.py` — Tests for ``ChainEvolver`` checkpoint / resume.
- `test_evolver_concurrent_evaluation.py` — Tests for concurrent per-individual evaluation in ``ChainEvolver``.
- `test_evolver_cost_estimate.py` — Tests for ``ChainEvolver.estimate_cost`` — pre-flight USD/token projection.
- `test_evolver_no_signal_warning.py` — Tests for the no-fitness-signal warning in ``ChainEvolver``.
- `test_evolver_population_metrics.py` — Tests for ``GenerationStats.population_metrics`` — per-individual runtime breakdown (score, wall_time_s, total_tokens, llm_calls).
- `test_evolver_smoke_check.py` — Tests for the ``ChainEvolver`` pre-flight smoke check.
- `test_examples_print_execution_summary.py` — Tests for ``examples.utils.print_execution_summary``.
- `test_format_mutation_effectiveness.py` — Tests for ``EvolutionResult.format_mutation_effectiveness``.
- `test_format_pareto.py` — Tests for ``EvolutionResult.format_pareto`` — per-individual score-vs-cost Pareto chart across all generations.
- `test_format_runs_pareto.py` — Tests for ``format_runs_pareto`` — cross-run Pareto front across a budget sweep of evolutions.
- `test_format_score_evolution.py` — Tests for ``EvolutionResult.format_score_evolution`` — per-generation score chart with best/mean/worst lines.
- `test_format_spend_vs_quality.py` — Tests for ``EvolutionResult.format_spend_vs_quality``.
- `test_from_description_prompt_types.py` — Tests that the ``ChainBuilder.from_description`` planning prompt declares field types explicitly so the LLM produces pydantic-valid output on the first attempt.
- `test_from_description_provenance.py` — Tests for planner-provenance capture.
- `test_from_description_retry.py` — Tests for ``ChainBuilder.from_description`` retry-with-feedback path.
- `test_legacy_chain_fixtures.py` — Legacy chain JSON fixture round-trip + typed ``ChainFormatNewerError`` exception.
- `test_migration_guide_examples.py` — Verify the migration guide examples.
- `test_mmar_carl.py` — Tests for mmar-carl library with mocked LLMHub.
- `test_multi_objective_evolution.py` — Tests for multi-objective ChainEvolver.
- `test_optional_deps.py` — Tests for optional dependency handling.
- `test_preflight.py` — Tests for Tool registry & pre-flight introspection.
- `test_reasoning_result_serialization.py` — Tests for ReasoningResult / StepExecutionResult serialization round-trip.
- `test_run_record.py` — Tests for RunRecord durable persistence model.
- `test_skill_output_schema.py` — Tests for AgentSkill LLM_AGENT structured output schema.
- `test_structural_mutations.py` — Tests for structural mutations.
- `test_to_lineage_mermaid.py` — Tests for ``EvolutionResult.to_lineage_mermaid``.
- `test_typed_result_data_accessors.py` — typed views over ``StepExecutionResult.result_data``.
- `test_viz_extras_gate.py` — Tests for the ``mmar-carl[viz]`` optional-extras gate.

_This file is generated by `scripts/generate_topic_readmes.py`. Run `make docs-topic-index` after adding or removing files._
