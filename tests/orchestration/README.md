# Orchestration Tests

DAG execution engine: dependency resolution, parallel batches, conditional branching, loops, tool steps, transform steps, step caching, auto-workers, builder helpers.

## Files

- `test_artifact_runtime_io.py` — Hermetic checks for the staged-workspace artifact runtime contract.
- `test_auto_workers.py` — Tests for max_workers="auto" parallel batch size auto-tuning.
- `test_builder_conditional_helpers.py` — Tests for ChainBuilder.add_if_else and add_switch convenience helpers.
- `test_cancellation.py` — Tests for mid-execution cancellation via context.cancel().
- `test_cancellation_intrastep.py` — Intra-step cancellation polling + snapshot propagation hardening.
- `test_command_capabilities.py` — Focused contract tests for host-owned command capabilities.
- `test_command_plan_steps.py` — Integration tests for typed command planning and planned execution.
- `test_command_steps.py` — Tests for the ``command`` step type (:class:`CommandStepExecutor`).
- `test_conditional_steps.py` — Tests for conditional step execution in CARL.
- `test_dynamic_step_injection.py` — Tests for dynamic step injection at runtime.
- `test_event_bus.py` — Tests for the intra-chain event bus.
- `test_execute_async_context_diagnostic.py` — Tests for the improved diagnostic when ``ReasoningChain.execute()`` is called from inside a running event loop.
- `test_execute_from_trace.py` — Tests for ``ReasoningChain.execute_from_trace``.
- `test_execute_resume_from.py` — `ReasoningChain.execute_async(resume_from=ContextSnapshot)`.
- `test_execution_profiling.py` — Tests for execution profiling hooks (StepExecutionResult.profiling and ReasoningResult.get_profiling_summary).
- `test_execution_trace.py` — Tests for ExecutionTrace and TraceEvent.
- `test_format_gantt.py` — Tests for ``ExecutionTrace.format_gantt`` — Gantt chart from trace.
- `test_loop_execution.py` — Tests for loop/cycle support in the DAG executor and ChainBuilder helpers.
- `test_managed_network_command_steps.py` — CommandStep integration tests for host-owned managed egress.
- `test_map_step.py` — Bounded ordered MapStep fan-out, aggregation, and lifecycle contracts.
- `test_partial_outputs_on_cancel.py` — Tests for partial-output recovery after ``ExecutionCancelledError``.
- `test_pause_resume.py` — Pause / resume API + ``ContextSnapshot``.
- `test_shell_session_artifacts.py` — Contract and local E2E tests for ShellSessionStep and portable artifacts.
- `test_step_caching.py` — Tests for step result caching (StepCache memoization).
- `test_step_groups.py` — Tests for ``StepGroup`` bulk LLM-config overrides.
- `test_stream_async.py` — Tests for streaming-first chain execution mode.
- `test_to_mermaid_critical_path.py` — Tests for ``ReasoningChain.to_mermaid_critical_path(result)``.
- `test_to_mermaid_heatmap.py` — Tests for ``ReasoningChain.to_mermaid_heatmap(result, metric=...)``.
- `test_wait_step.py` — Self-contained WaitStep timers, events, composition and cancellation.

_This file is generated by `scripts/generate_topic_readmes.py`. Run `make docs-topic-index` after adding or removing files._
