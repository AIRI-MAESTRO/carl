# Evaluation Tests

Step metrics, dataset evaluators, evaluation step types, and result formatters — including the visualization methods on `ReasoningResult` and `DatasetEvaluationReport` (cost-by-model, failure heatmap, step-metric heatmap, profiling tables, token pies).

## Files

- `test_dataset.py` — Unit tests for dataset abstractions (models/dataset.py).
- `test_dataset_evaluator.py` — Integration tests for DatasetEvaluator and dataset_report in ReflectionOptions.
- `test_eval_suite.py` — Tests for ``EvalSuite``.
- `test_evaluation_step.py` — Tests for EvaluationStep — inline quality gate.
- `test_format_cost_by_model.py` — Tests for ``ReasoningResult.format_cost_by_model``.
- `test_format_cost_trend.py` — Tests for ``DatasetEvaluationReport.format_cost_trend``.
- `test_format_failure_heatmap.py` — Tests for ``DatasetEvaluationReport.format_failure_heatmap``.
- `test_format_latency_histogram.py` — Tests for ``DatasetEvaluationReport.format_latency_histogram``.
- `test_format_profiling_table.py` — Tests for ``ReasoningResult.format_profiling_table``.
- `test_format_prompt_completion_breakdown.py` — Tests for ``ReasoningResult.format_prompt_completion_breakdown`` — per-step stacked bar showing prompt-tokens vs completion-tokens.
- `test_format_score_distribution.py` — Tests for ``DatasetEvaluationReport.format_score_distribution``.
- `test_format_step_metric_heatmap.py` — Tests for ``DatasetEvaluationReport.format_step_metric_heatmap``.
- `test_format_token_pie.py` — Tests for ``ReasoningResult.format_token_pie`` — text / Mermaid / PNG rendering of per-step token spend.
- `test_metric_case_aware.py` — Tests for case-aware metric dispatch in ``DatasetEvaluator`` and the public ``call_metric_async`` helper.
- `test_metrics.py` — Tests for the MetricBase evaluation system.
- `test_reflection.py` — Tests for reflection feature.
- `test_token_usage_by_step.py` — Tests for ``ReasoningResult.token_usage_by_step`` convenience accessor.

_This file is generated by `scripts/generate_topic_readmes.py`. Run `make docs-topic-index` after adding or removing files._
